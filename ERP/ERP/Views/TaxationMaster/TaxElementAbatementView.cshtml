@model List<ERP_DTO.TaxElementDetail_DTO>

<div class="w-100 table-responsive">
    <table class="table table-bordered align-middle table-hover align-middle mb-0 table-grid">
        <thead>
            <tr class="table-info">
                <th class="datetype text-center">From Date</th>
                <th class="datetype text-center">To Date</th>
                <th class="table-width-lg text-end">Fixed Percent</th>
                <th class="action text-center">Action</th>
            </tr>
        </thead>
        <tbody id="AbTableBody">
            <tr id="AbTempRow" style="display: none;">
                <td class="datetype">
                    <input id="FromDate" class="form-control text-center FromDate datepicker" autocomplete="off" style="max-width:120px !important" />
                </td>
                <td class="datetype">
                    <input id="ToDate" class="form-control text-center ToDate datepicker" autocomplete="off" style="max-width:120px !important" />
                </td>
                <td class="table-width-lg">
                    <div class="d-flex align-items-center">
                        <input id="FixedPercent" class="form-control decimal FixedPercent text-end" autocomplete="off" />
                        <span class="percent-symbol">%</span>
                    </div>
                </td>
                <td class="action text-center">
                    <input type="hidden" name="IsDeleted" value="false" class="IsDeleted" />
                    <a class="AbRowRemove btn text-danger p-0 icon-link shadow-none">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash3-fill" viewBox="0 0 16 16">
                            <path d="M11 1.5v1h3.5a.5.5 0 0 1 0 1h-.538l-.853 10.66A2 2 0 0 1 11.115 16h-6.23a2 2 0 0 1-1.994-1.84L2.038 3.5H1.5a.5.5 0 0 1 0-1H5v-1A1.5 1.5 0 0 1 6.5 0h3A1.5 1.5 0 0 1 11 1.5m-5 0v1h4v-1a.5.5 0 0 0-.5-.5h-3a.5.5 0 0 0-.5.5M4.5 5.029l.5 8.5a.5.5 0 1 0 .998-.06l-.5-8.5a.5.5 0 1 0-.998.06m6.53-.528a.5.5 0 0 0-.528.47l-.5 8.5a.5.5 0 0 0 .998.058l.5-8.5a.5.5 0 0 0-.47-.528M8 4.5a.5.5 0 0 0-.5.5v8.5a.5.5 0 0 0 1 0V5a.5.5 0 0 0-.5-.5" />
                        </svg>
                    </a>
                </td>
            </tr>
            @if (Model != null)
            {
                @for (int i = 0; i < Model.Count; i++)
                {
                    <tr class="AbNewRow">
                        <td class="datetype">
                            <input id="FromDate" name="Abatement_List[@i].FromDate" class="form-control text-center FromDate datepicker" autocomplete="off" value="@Model[i].FromDate" style="max-width:120px !important" />
                        </td>
                        <td class="datetype text-center">
                            <input id="ToDate" name="Abatement_List[@i].ToDate" class="form-control text-center ToDate datepicker" autocomplete="off" value="@Model[i].ToDate" style="max-width:120px !important" />
                        </td>
                        <td class="table-width-lg">
                            <div class="d-flex align-items-center">
                                <input id="FixedPercent" name="Abatement_List[@i].FixedPercent" class="form-control decimal FixedPercent text-end" autocomplete="off" value="@Model[i].FixedPercent" />
                                <span class="percent-symbol">%</span>
                            </div>
                        </td>
                        <td class="action text-center">
                            @if (@Model[i].TaxNumber != 0)
                            {
                                <input name="Abatement_List[@i].TaxNumber" type="hidden" class="TaxNumber" value="@Model[i].TaxNumber" />
                            }
                            <input type="hidden" name="Abatement_List[@i].IsDeleted" value="false" />
                            <a class="AbRowRemove btn text-danger p-0 icon-link shadow-none">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-trash3-fill" viewBox="0 0 16 16">
                                    <path d="M11 1.5v1h3.5a.5.5 0 0 1 0 1h-.538l-.853 10.66A2 2 0 0 1 11.115 16h-6.23a2 2 0 0 1-1.994-1.84L2.038 3.5H1.5a.5.5 0 0 1 0-1H5v-1A1.5 1.5 0 0 1 6.5 0h3A1.5 1.5 0 0 1 11 1.5m-5 0v1h4v-1a.5.5 0 0 0-.5-.5h-3a.5.5 0 0 0-.5.5M4.5 5.029l.5 8.5a.5.5 0 1 0 .998-.06l-.5-8.5a.5.5 0 1 0-.998.06m6.53-.528a.5.5 0 0 0-.528.47l-.5 8.5a.5.5 0 0 0 .998.058l.5-8.5a.5.5 0 0 0-.47-.528M8 4.5a.5.5 0 0 0-.5.5v8.5a.5.5 0 0 0 1 0V5a.5.5 0 0 0-.5-.5" />
                                </svg>
                            </a>
                        </td>
                    </tr>
                }
            }

        </tbody>
    </table>
</div>
<script>
    $(document).ready(function () {
         function InitializeFlatpickrs() {
             $(".datepicker").flatpickr({
                 dateFormat: "d-M-Y",
                 altInput: false,
             });
         }

        var rowCount = @(Model != null ? Model.Count : 0);
        var isAddingRow = false;

        function addRow() {
            var AbNewRow = $("#AbTempRow").clone().removeAttr("style").removeAttr("id").addClass("AbNewRow");
            AbNewRow.find("input.FromDate").attr("name", "Abatement_List[" + rowCount + "].FromDate");
            AbNewRow.find("input.ToDate").attr("name", "Abatement_List[" + rowCount + "].ToDate");
            AbNewRow.find("input.FixedPercent").attr("name", "Abatement_List[" + rowCount + "].FixedPercent").val("0.00");
            AbNewRow.find("input.IsDeleted").attr("name", "Abatement_List[" + rowCount + "].IsDeleted");

            AbNewRow.find("input[name='Abatement_List[" + rowCount + "].IsDeleted']").val(false);

            $("#AbTableBody").append(AbNewRow);
            rowCount++;
            AbNewRow.data("isValid", false);
            isAddingRow = false;

            InitializeFlatpickrs();
        }

        if ($("tr.AbNewRow").length == 0) {
            addRow();
        }

        $(document).on('click', '.AbRowRemove', function () {
            $(this).closest('tr').find("input[name*='IsDeleted']").val(true);
            $(this).closest('tr').hide();
        });

        $(document).on('keydown', "tr.AbNewRow select, tr.AbNewRow input.FromDate, tr.AbNewRow input.ToDate,tr.AbNewRow input.FixedPercent, tr.AbNewRow input[type='text']", function (event) {
            if (event.keyCode === 9) {
                if (!validateField($(this)))
                    event.preventDefault();
            }
        });

        $(document).on("change", "tr.AbNewRow select, tr.AbNewRow input.FromDate, tr.AbNewRow input.ToDate, tr.AbNewRow input.FixedPercent, input[type='text']", function () {
            if (isAddingRow)
                return;
            isAddingRow = true;

            var currentInput = $(this);
            var isValid = validateField(currentInput);
            var currentRow = $(this).closest("tr.AbNewRow");
            if (isValid) {
                if (checkAllFieldsValid(currentRow)) {
                    currentRow.data("isValid", true);
                }
                else {
                    currentRow.data("isValid", false);
                }
            }
            else {
                currentRow.data("isValid", false);
            }
            if (currentRow.data("isValid")) {
                var lastRow = $("tr.AbNewRow").last();
                if (checkAllFieldsValid(lastRow))
                    addRow();
            }
            else {
                isAddingRow = false;
            }
        });

        function checkAllFieldsValid(currentRow) {
            var allFieldsValid = true;
            $("tr.AbNewRow:visible").each(function () {
                $(this).find("select, input.FromDate, input.ToDate,input.FixedPercent, input[type='text']").each(function () {
                    if (!validateField($(this))) {
                        allFieldsValid = false;
                        return false;
                    }
                });
                if (!allFieldsValid) return false;
            });
            return allFieldsValid;
        }


        function validateField(currentInput) {
            var valid = 1;
            var inputValue = currentInput.val();
            if (currentInput.hasClass("FromDate") || currentInput.hasClass("ToDate")) {
                valid = validateDateRange(currentInput);
            }
            if (inputValue === "") {
                valid = 0;
            }

            if (currentInput.hasClass("FixedPercent")) {
                valid = validateAmount(currentInput);
            }

            if (valid == 0) {
                return false;
            }
            else {
                return true;
            }
        }

        function validateDateRange(currentInput) {
            var valid = 1;
            var currentRow = currentInput.closest("tr.AbNewRow");
            var fromDateInput = currentRow.find("input.FromDate");
            var toDateInput = currentRow.find("input.ToDate");
            var fromDate = fromDateInput.val();
            var toDate = toDateInput.val();
            if (fromDate === "" || toDate === "") {
                valid = 0;
            }
            var from = new Date(fromDate);
            var to = new Date(toDate);
            if (to < from) {
                alert("'To Date' must be greater than or equal to 'From Date'");
                currentInput.val("");
                valid = 0;
            }
            return valid;
        }
        function validateAmount(currentInput) {
            var valid = 1;

            var currentRow = currentInput.closest("tr.AbNewRow");
                var FixedPercentInput = currentRow.find("input.FixedPercent");
                var FixedPercent = FixedPercentInput.val();
                if (FixedPercent > 0) {
                }
                else {
                    valid = 0;
                }
            return valid;
        }

        $("#AbOkayButton").click(function () {
               GridValidation();
             });
        $("#AbCloseButton").click(function () {
               GridValidation();
             });
            function GridValidation(){
                $("tr.AbNewRow").each(function () {
                var fromDate = $(this).find("input.FromDate").val();
                var toDate = $(this).find("input.ToDate").val();
                var fixedPercent = $(this).find("input.FixedPercent").val();

                if (!fromDate && !toDate && !(fixedPercent>0)) {
                    $(this).remove();
                }
            });


            var lastRow = $("tr.AbNewRow").last();
            if (checkAllFieldsValid(lastRow)) {

                $("#AbatementModel").modal('hide');
            }
            else {
                alert("Please complete all fields !");
                firstInvalidField.focus();
            }
       }
            // var lastRow = $("tr.AbNewRow").last();
            // var firstInvalidField = null;
            // lastRow.find("select, input.FromDate, input.ToDate,input.FixedPercent, input[type='text']").each(function () {
            //     if (!validateField($(this))) {
            //         if (firstInvalidField == null)
            //             firstInvalidField = $(this);
            //         return false;
            //     }
            // });
            // if (firstInvalidField != null) {
            //     alert("Please complete all fields !");
            //     firstInvalidField.focus();
            // }
            // else {
            //     $("#AbatementModel").modal('hide');
            // }

        $("#AbAddRowButton").click(function () {
            var lastRow = $("tr.AbNewRow").last();
            if (checkAllFieldsValid(lastRow)) {
                addRow();
            }
            else {
                var firstInvalidField = null;
                lastRow.find("select, input.FromDate, input.ToDate,input.FixedPercent, input[type='text']").each(function () {
                    if (!validateField($(this))) {
                        if (firstInvalidField == null)
                            firstInvalidField = $(this);
                        return false;
                    }
                });
                if (firstInvalidField != null) {
                    alert("Please complete all fields !");
                    firstInvalidField.focus();
                }
            }
        });
    });
</script>